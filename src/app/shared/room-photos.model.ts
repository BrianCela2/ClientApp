 export interface CreateRoomPhotoDTO{
    RoomId:string; 
    ImageFile?: File[];
    }

export interface RoomPhotoDTO{
            photoId:string; 
            photoPath:string;
            photoContent:string;
 }
    
